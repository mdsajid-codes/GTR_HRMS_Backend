master:
    datasource:
        url: jdbc:mysql://localhost:3306/master_db
        username: root
        password : Sajid7254@

provisioning:
    datasource:
        host: localhost
        username: root
        password: Sajid7254@

spring:
  jpa:
    defer-datasource-initialization: true
    open-in-view: false
    show-sql: true
  sql:
    init:
      mode: never
  spring:
    servlet:
      multipart:
        max-file-size: 10MB
        max-request-size: 10MB
  