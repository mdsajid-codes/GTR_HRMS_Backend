master:
  datasource:
    url: jdbc:mysql://209.209.40.85:19783/master_db
    username: admin
    password: gqa6vDtX

provisioning:
  datasource:
    host: "209.209.40.85"
    username: admin
    password: gqa6vDtX
    port: "19783"

spring:
  jpa:
    defer-datasource-initialization: true
    open-in-view: false
    show-sql: true
  sql:
    init:
      mode: never
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  mail:
    host: smtp.example.com
    port: 587
    username: your-email@example.com
    password: your-email-password
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

app:
  company:
    default-email: sales@yourcompany.com

server:
  port: 8080 # Default HTTP port
  # ssl:
  #   enabled: false # Disabled for local development
  #   key-store-type: PKCS12
  #   key-store: classpath:keystore.p12
  #   key-store-password: password
  #   key-alias: myapp
# jdbc:mysql://209.209.40.85:19783/master_db
# admin
# gqa6vDtX
