master:
    datasource:
        url: jdbc:mysql://localhost:3306/master_db
        username: root
        password : Sajid7254@

spring:
  jpa:
    defer-datasource-initialization: true
    open-in-view: false
    show-sql: true
  sql:
    init:
      mode: never